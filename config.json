{
    "name": "Instance Marker",

    "namespace": "DCC\\Instance_tagger",

    "description": "Clearly differentiate REDCap instances. A short text describing the REDCap instance is displayed in the upper right corner of every page, and every project page (if enabled). The URL is mapped to instances such as 'Localhost', 'Development', 'Testing', and 'Backup'. The tab title is prefixed with the text for the specific instance. The module must be enabled on all projects by default in order for the instance label to appear inside projects or enabled on a project by project basis.",

    "authors": [
        {
            "name": "Greg Neils",
            "email": "gn2003@cumc.columbia.edu",
            "institution": "Columbia University"
        }
    ],

    "permissions": [
        "redcap_every_page_top"
    ],

    "system-settings": [
        {
            "key": "localhost_url",
            "name": "Localhost URL",
            "required": false,
            "type": "text"
        },
        {
            "key": "development_url",
            "name": "Development URL",
            "required": false,
            "type": "text"
        },
        {
            "key": "testing_url",
            "name": "Testing URL",
            "required": false,
            "type": "text"
        },
        {
            "key": "backup_url",
            "name": "Backup URL",
            "required": false,
            "type": "text"
        },
        {
            "key": "staging_url",
            "name": "Staging URL",
            "required": false,
            "type": "text"
        },
        {
            "key": "other_url",
            "name": "Other URL",
            "required": false,
            "type": "text"
        },
        {
            "key": "other_text",
            "name": "Custom Text to display for 'Other URL'",
            "required": false,
            "type": "text"
        },
        {
            "key": "offset_top",
            "name": "Location (fixed): Pixels from the TOP of the page. Leave blank for default",
            "required": false,
            "type": "text"
        },
        {
            "key": "offset_right",
            "name": "Location (fixed): Pixels from the RIGHT of the page. Leave blank for default",
            "required": false,
            "type": "text"
        },
        {
            "key": "offset_bottom",
            "name": "Location (fixed): Pixels from the BOTTOM of the page. Leave blank for default",
            "required": false,
            "type": "text"
        },
        {
            "key": "offset_left",
            "name": "Location (fixed): Pixels from the LEFT of the page. Leave blank for default",
            "required": false,
            "type": "text"
        },
        {
            "key": "width",
            "name": "Width of the marker in pixels. Min: 15, Max: 1000. Leave blank for default",
            "required": false,
            "type": "text"
        },
        {
            "key": "height",
            "name": "Height of the marker in pixels. Min: 15 Max: 800. Leave blank for default",
            "required": false,
            "type": "text"
        },
        {
            "key": "bk_color",
            "name": "Background color. Specify HTML color code or leave blank to use default colors.",
            "required": false,
            "type": "text"
        },
        {
            "key": "opacity",
            "name": "Opacity %: Min: 0, Max: 100.",
            "required": false,
            "type": "text"
        }
    ],

    "project-settings": [
    ]
}
